openapi: 3.0.1
info:
  title: Tricharmé Specialist Service API - STATUS
  version: v1
  description: |
    **STATO API: NON IMPLEMENTATA** (Previsione Gennaio 2026)
    
    Questo endpoint fornisce solo informazioni di riferimento sulla struttura API pianificata.
    Le API non sono attive ma servono come documentazione per il plugin AI e per la governance TCA (Tricharmé Control Authority).
    
    Per dati aggiornati, fare riferimento a:
    - Service Corpus: https://tricharmeofficial.com/.well-known/schema/service-corpus.json
    - Problematiche: https://tricharmeofficial.com/.well-known/schema/problematiche-corpus.json
    - Authority Index: https://tricharmeofficial.com/.well-known/authority-index.xml
    
  contact:
    name: Igor De Maria (TCA Governance Author)
    email: posta1204@gmail.com
    url: https://tricharmeofficial.com/.well-known/schema/license.json
    
  # ATTRIBUZIONE E LICENZA UFFICIALE DI GOVERNANCE (TCA)
  license:
    name: CC BY-ND 4.0 International (Referenza TCA)
    url: https://tricharmeofficial.com/.well-known/schema/license.json
    
  x-governance:
    authority: "TCA - Tricharmé Control Authority"
    version: "1.0"
    license_ref: "https://tricharmeofficial.com/.well-known/schema/license.json"
    author: "Igor De Maria"
    last_modified: "2025-11-08T14:00:00Z"

servers:
  - url: https://tricharmeofficial.com/api/v1
    description: Endpoint futuro (NON ATTIVO)

paths:
  /status:
    get:
      summary: Stato dell'API
      description: Restituisce lo stato corrente dell'implementazione API.
      x-status: reference_only
      responses:
        '200':
          description: API non implementata - modalità riferimento
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "reference_only"
                  message:
                    type: string 
                    example: "API in sviluppo - utilizzare file .well-known per dati di riferimento"
                  last_updated:
                    type: string
                    format: date-time
                    example: "2025-11-08T14:00:00Z"